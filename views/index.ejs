<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Crypto Market Insights Dashboard</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
            color: #e6e6e6;
            line-height: 1.6;
            padding: 20px;
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
        }
        
        header {
            text-align: center;
            padding: 30px 0;
            margin-bottom: 30px;
            border-bottom: 2px solid #0f3460;
        }
        
        h1 {
            font-size: 2.5rem;
            color: #e94560;
            margin-bottom: 10px;
        }
        
        .subtitle {
            font-size: 1.2rem;
            color: #4cc9f0;
            opacity: 0.9;
        }
        
        .controls {
            text-align: center;
            margin-bottom: 30px;
        }
        
        .add-insight-btn {
            background: linear-gradient(45deg, #0f3460, #1a5a8d);
            color: white;
            border: none;
            padding: 15px 30px;
            border-radius: 6px;
            cursor: pointer;
            font-size: 1.1rem;
            font-weight: bold;
            text-decoration: none;
            display: inline-block;
            transition: background 0.3s ease;
        }
        
        .add-insight-btn:hover {
            background: linear-gradient(45deg, #e94560, #ff6b81);
        }
        
        .insights-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
            gap: 25px;
            margin-top: 20px;
        }
        
        .insight-card {
            background: rgba(25, 25, 43, 0.8);
            border-radius: 12px;
            padding: 25px;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
            border: 1px solid rgba(76, 201, 240, 0.2);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        
        .insight-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 12px 40px rgba(233, 69, 96, 0.3);
        }
        
        .insight-date {
            color: #4cc9f0;
            font-weight: bold;
            font-size: 1.1rem;
            margin-bottom: 15px;
            display: block;
        }
        
        .insight-excerpt {
            color: #a9a9b3;
            margin-bottom: 20px;
            line-height: 1.7;
        }
        
        .read-more-btn {
            background: linear-gradient(45deg, #0f3460, #1a5a8d);
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 6px;
            cursor: pointer;
            font-weight: bold;
            transition: background 0.3s ease;
            text-decoration: none;
            display: inline-block;
        }
        
        .read-more-btn:hover {
            background: linear-gradient(45deg, #e94560, #ff6b81);
        }
        
        .no-insights {
            text-align: center;
            padding: 50px;
            color: #a9a9b3;
            font-size: 1.2rem;
        }
        
        footer {
            text-align: center;
            margin-top: 50px;
            padding: 20px;
            color: #a9a9b3;
            font-size: 0.9rem;
            border-top: 1px solid #0f3460;
        }
        
        @media (max-width: 768px) {
            .insights-grid {
                grid-template-columns: 1fr;
            }
            
            h1 {
                font-size: 2rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>💰 Crypto Market Insights Dashboard</h1>
            <p class="subtitle">Your curated collection of market research and analysis</p>
        </header>
        
        <div class="controls">
            <a href="/add-insight" class="add-insight-btn">➕ Add New Insight</a>
        </div>
        
        <% if (insights.length > 0) { %>
            <div class="insights-grid">
                <% insights.forEach(insight => { %>
                    <div class="insight-card">
                        <span class="insight-date"><%= insight.date %></span>
                        <h3><%= insight.filename %></h3>
                        <p class="insight-excerpt"><%= insight.excerpt %></p>
                        <a href="/insight/<%= insight.filename %>" class="read-more-btn">Read Full Analysis</a>
                    </div>
                <% }); %>
            </div>
        <% } else { %>
            <div class="no-insights">
                <p>No market insights found yet.</p>
                <p>Create your first research digest using the pattern: <code>research-digest-yyyy-mm-dd.md</code></p>
            </div>
        <% } %>
        
        <footer>
            <p>Powered by Jr (Junior) | Created for Jay | <%= new Date().getFullYear() %></p>
        </footer>
    </div>
</body>
</html>